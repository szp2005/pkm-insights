---
import BaseLayout from '../../layouts/BaseLayout.astro';

const modules = import.meta.glob('../../../content/posts/*.md', { eager: true });
const posts = Object.values(modules).map(mod => (mod as any).frontmatter ?? {});

const authorCounts: Record<string, number> = {};
posts.forEach(p => {
  if (p.author) {
    authorCounts[p.author] = (authorCounts[p.author] ?? 0) + 1;
  }
});

const authors = Object.entries(authorCounts).map(([name, count]) => ({
  name,
  count,
  slug: name.toLowerCase().replace(/\s+/g, '-')
})).sort((a, b) => b.count - a.count);
---
<BaseLayout
  title="Authors | PKM Insights"
  description="Meet the expert contributors behind PKM Insights' knowledge management guides."
  canonicalURL="https://pkm.notes-automate.com/author/"
>
  <div class="authors-page">
    <div class="page-heading">
      <h1>Our Authors</h1>
      <p>PKM researchers and knowledge management specialists.</p>
    </div>
    <div class="author-grid">
      {authors.map(author => (
        <div class="author-card">
          <h2><a href={`/author/${author.slug}/`}>{author.name}</a></h2>
          <p>{author.count} article{author.count !== 1 ? 's' : ''} published</p>
        </div>
      ))}
    </div>
  </div>
</BaseLayout>

<style>
.authors-page { max-width: 820px; margin: 0 auto; padding: 2rem 1rem; }
.page-heading { padding: 2rem 0 2rem; }
.page-heading h1 { font-size: 2rem; margin-bottom: 0.5rem; }
.page-heading p { color: #6b7280; font-size: 1.05rem; margin: 0; }
.author-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 1.5rem; margin-top: 2rem; }
.author-card { padding: 1.5rem; border: 1px solid #e5e7eb; border-radius: 8px; transition: border-color 0.2s; }
.author-card:hover { border-color: #16a34a; }
.author-card h2 { margin: 0 0 0.5rem 0; font-size: 1.25rem; }
.author-card a { color: #16a34a; text-decoration: none; }
.author-card a:hover { text-decoration: underline; }
.author-card p { margin: 0; color: #6b7280; font-size: 0.9rem; }
</style>
